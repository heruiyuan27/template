spring:
  profiles:
    active: dev
  web:
    resources:
      static-locations: classpath:/static,classpath:/public,classpath:/resources,classpath:/META-INF/resources

server:
  port: 8080
  servlet:
    context-path: /app

knife4j:
  enable: true
  production: false